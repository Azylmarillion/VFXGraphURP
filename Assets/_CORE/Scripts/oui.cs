using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class oui : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print(Mathf.Pow(2, 2f));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
